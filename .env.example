PORT=3000
DB_URL="host=localhost port=5432 dbname=developdb01 sslmode=disable"
SECRET=secret-key-for-jwt-authentication

# Firebase Cloud Messaging (push notifications)
# Path to the Firebase Admin SDK service account JSON file.
# Leave blank to fall back to Application Default Credentials.
FIREBASE_SERVICE_ACCOUNT_FILE=/path/to/firebase-service-account.json
# Set to "true" when sending iOS notifications to development/TestFlight builds,
# "false" (or unset) for production App Store builds.
APNS_USE_SANDBOX=true

# Resend (transactional email: password reset, etc.)
RESEND_API_KEY=re_your_resend_api_key
RESEND_FROM_EMAIL=PrayerLoop <noreply@yourdomain.com>

# Planning Center OAuth (backend-mediated token exchange — planning doc §D/§G)
# Register at https://api.planningcenteronline.com/oauth/applications
PC_CLIENT_ID=your_planning_center_client_id
PC_CLIENT_SECRET=your_planning_center_client_secret
# AES-256-GCM key (base64, 32 bytes) for encrypting provider tokens at rest.
# Generate a separate key per environment, e.g.: openssl rand -base64 32
OAUTH_TOKEN_ENC_KEY=your_base64_32_byte_key

# Prayerloop server-side refresh token lifetime (days). Defaults to 90 if unset.
REFRESH_TOKEN_TTL_DAYS=90

# Google OAuth (Sign in with Google — planning doc §M.3/§F.6)
# Register at https://console.cloud.google.com/apis/credentials
GOOGLE_OAUTH_CLIENT_ID=your_google_oauth_client_id
GOOGLE_OAUTH_CLIENT_SECRET=your_google_oauth_client_secret

# Apple OAuth (Sign in with Apple — planning doc §M.3/§F.6)
# Register at https://developer.apple.com/account/resources/identifiers/list/serviceId
APPLE_CLIENT_ID=your_apple_services_id
APPLE_TEAM_ID=your_apple_team_id
APPLE_KEY_ID=your_apple_key_id
# .p8 private key content (PEM), e.g. the output of `cat AuthKey_XXXXXXXXXX.p8`.
# Used to sign a short-lived client_secret JWT per request — not a static secret.
APPLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
# Comma-separated app bundle IDs, in addition to APPLE_CLIENT_ID, accepted as
# an id_token's audience. Native Sign in with Apple (expo-apple-authentication)
# audiences the id_token to the app's bundle ID, not the Services ID above.
APPLE_BUNDLE_IDS=com.example.app,com.example.app.dev

# RevenueCat (subscription paywall — planning doc §D)
# HMAC secret from the RC dashboard (Integrations -> Webhooks) — NOT the same
# as the API key below. Used to verify POST /webhooks/revenuecat deliveries.
REVENUECAT_WEBHOOK_SIGNING_SECRET=your_revenuecat_webhook_signing_secret
# Server-side secret key (Project settings -> API keys). Used only by the
# /sync REST-pull endpoint — NEVER ship this in the mobile app bundle.
REVENUECAT_SECRET_API_KEY=sk_your_revenuecat_secret_api_key
# Free-tier prayer circle cap. Env var so ops can tune it without a redeploy.
FREE_CIRCLE_LIMIT=3
# Budget-charged circle restores per ROLLING 30 days, free tier only.
# Premium users and admins are exempt -- they have no circle limit to rotate
# around, so metering them would be friction with no purpose. Restores inside
# the undo window (below) are always free and never logged.
RESTORE_BUDGET=3
# How long after archiving a restore is free and unlogged. This is the only
# thing user_group.archived_at exists for: it lets the server tell a
# ten-second fat-finger from a three-week-old archive, so mis-tapping a row in
# the bulk archive chooser doesn't permanently cost a restore.
# Set to 0 to make EVERY restore chargeable -- necessary for testing the budget
# by hand, since Bruno/curl always archive and restore inside 5 minutes and the
# metering therefore looks broken when it is working correctly.
RESTORE_UNDO_WINDOW_SECONDS=300
# Kill switch for the mobile over-limit entry gate (the forced
# archive-or-subscribe choice a free user meets when their active circle count
# EXCEEDS the limit). Set false to disarm it everywhere without a mobile
# release -- worth knowing that an OTA reaches every build on the same SDK
# version at once, and this gate is the one behavior that can lock a user out
# of circles they already have.
# NOTE: raising FREE_CIRCLE_LIMIT alone does NOT disarm the gate for someone
# already over the new limit, which is why this is a separate switch.
CIRCLE_ENTRY_GATE_ENABLED=true
